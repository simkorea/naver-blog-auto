# 클로드 코드로 넘기기 — 사장님용 안내

이제부터는 **제 PC 안에서** 클로드가 직접 명령어를 실행합니다.
캡처해서 보내고, 고쳐진 ZIP 을 다시 받고 하는 왕복이 없어집니다.

한 번만 설치하면 됩니다. 15분쯤 걸립니다.

---

## 1단계 · Git 설치 (5분)

프로그램을 ZIP 으로 받지 않고 `git` 으로 받으면, 앞으로는 **폴더가 늘어나지 않고
같은 자리에서 갱신**됩니다. `(2)`, `(3)` 폴더가 생기던 문제가 사라집니다.

https://git-scm.com/downloads/win 접속 → **Click here to download** →
받은 파일 실행 → **계속 [Next]** 만 누르시면 됩니다. 설정은 기본값 그대로 두세요.

> 클로드 코드가 윈도우에서 명령어를 실행하려면 이게 있는 편이 좋습니다.
> 없어도 동작은 하지만, 있으면 훨씬 매끄럽습니다.

---

## 2단계 · 클로드 코드 설치 (5분)

**두 가지 방법이 있습니다. 첫 번째를 권합니다.**

### 방법 A — 데스크톱 앱 (권장, 터미널 불필요)

https://claude.com/download 접속 → 윈도우용 받기 → 설치 → 로그인

지금 쓰시는 것과 같은 계정으로 로그인하시면 됩니다.
검은 명령창을 쓸 일이 없어서 사장님께는 이쪽이 편합니다.

### 방법 B — 명령창으로 설치

윈도우 검색에 `powershell` 입력 → **Windows PowerShell** 실행 → 아래 한 줄 붙여넣기:

```powershell
irm https://claude.ai/install.ps1 | iex
```

설치 후 확인:

```powershell
claude --version
```

`2.x.x (Claude Code)` 같은 게 나오면 성공입니다.

> **필요한 것:** Claude Pro, Max, Team, Enterprise 중 하나의 구독.
> 지금 저와 대화하고 계시니 이미 있으실 겁니다.

---

## 3단계 · 프로그램 받기 (2분)

PowerShell 또는 명령 프롬프트에서 (한 줄씩):

```
cd %USERPROFILE%
git clone https://github.com/simkorea/naver-blog-auto.git
cd naver-blog-auto
git checkout claude/file-voice-analysis-program-lb7qi8
```

> PowerShell 에서 `cd %USERPROFILE%` 가 안 되면 `cd $env:USERPROFILE` 로 하세요.

이러면 `C:\Users\sims\naver-blog-auto` 에 프로그램이 자리잡습니다.
**앞으로는 이 폴더 하나만 씁니다.** 새 버전은 `git pull` 한 줄이면 갱신됩니다.

---

## 4단계 · 클로드 코드 시작

**데스크톱 앱을 쓰신다면:** 앱을 열고 방금 만든 `naver-blog-auto` 폴더를 여세요.

**명령창을 쓰신다면:**

```
cd %USERPROFILE%\naver-blog-auto
claude
```

첫 실행 때 브라우저가 열리면서 로그인을 물어봅니다. 승인하시면 됩니다.

---

## 5단계 · 첫 지시

클로드 코드가 뜨면, 아래를 **그대로 복사해서** 붙여넣으세요:

```
CLAUDE.md 를 읽고 지금 상태를 파악해줘.
그다음 python evidence/setup_check.py --go 를 직접 실행해서
화자 분리 모델 받기와 자체 점검까지 끝내줘.
오류가 나면 실제 라이브러리 소스를 열어서 원인을 확인하고 고쳐줘.
```

`CLAUDE.md` 에 지금까지 겪은 문제와 해결책, 건드리면 안 되는 것들을 전부
적어두었습니다. 처음부터 다시 헤매지 않습니다.

---

## 알아두실 것

**허락을 물어봅니다.** 파일을 고치거나 명령어를 실행할 때 처음에는 매번
물어봅니다. `Yes` 를 누르시면 됩니다. 익숙해지면 `Shift+Tab` 으로
덜 물어보게 바꿀 수 있습니다.

**증거 자료는 안전합니다.** 증거 DB·인증키·사건 사전은 프로그램 폴더가 아니라
`C:\Users\sims\EvidenceFinder\` 에 있습니다. 프로그램을 갱신해도 그대로입니다.

**저(이 대화)는 계속 씁니다.** 큰 방향을 잡거나 막힐 때는 여기서 상의하시고,
실제 실행은 PC 안의 클로드 코드가 하는 식으로 나누시면 됩니다.

---

## 막히면

| 증상 | 해결 |
|---|---|
| `'claude' 은(는) 명령어가 아닙니다` | 명령창을 껐다 다시 여세요. 그래도 안 되면 데스크톱 앱(방법 A)으로 하세요. |
| `'git' 은(는) 명령어가 아닙니다` | 1단계 Git 설치 후 명령창을 껐다 다시 여세요. |
| PowerShell 에서 `&&` 오류 | 한 줄씩 따로 실행하세요. |
| 로그인이 안 됨 | 구독(Pro/Max) 계정으로 로그인하셨는지 확인하세요. |

그래도 막히시면 화면 캡처해서 저에게 보여주세요.
