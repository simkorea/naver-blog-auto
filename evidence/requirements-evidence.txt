# Evidence Finder dependencies
#
#   python -m pip install -r evidence/requirements-evidence.txt
#
# NOTE: This file is intentionally ASCII-only.
# pip reads requirement files using the system locale encoding when there is
# no BOM. On a Korean Windows machine that is cp949, so any UTF-8 comment here
# crashes pip with UnicodeDecodeError before a single package is installed.
# Keep every character in this file ASCII.
#
# GPU users: install torch first so you get the CUDA build.
#   python -m pip install torch --index-url https://download.pytorch.org/whl/cu124
# Or just run:  python evidence/setup_check.py --install

# UI
streamlit>=1.40

# Speech recognition
faster-whisper>=1.1.0

# Speaker diarization (optional - skipped if missing)
pyannote.audio>=3.3.0

# Semantic search (optional - keyword search still works without it)
sentence-transformers>=3.0.0
sqlite-vec>=0.1.6

# Document text extraction
pdfplumber>=0.11.0
pypdfium2>=4.30.0
python-docx>=1.1.0
openpyxl>=3.1.0

# Image OCR (shared with image_fetcher.py)
easyocr>=1.7.0
Pillow>=10.0.0

# Audio clip extraction (bundles the ffmpeg binary)
imageio-ffmpeg>=0.5.1

# Statute and precedent lookup
requests>=2.31.0

# Configuration
PyYAML>=6.0
python-dotenv>=1.0.0

# Optional LLM tagging (only used when OFFLINE_ONLY=false)
google-generativeai>=0.8.0
