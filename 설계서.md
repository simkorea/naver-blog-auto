# 네이버 블로그 자동화 시스템 설계서

> 최종 업데이트: 2026-04-29

---

## 1. 시스템 개요

네이버 부동산 전문 블로그의 원고 생성 → 이미지 수집 → 업로드를 자동화하는 파이프라인.

```
터미널 실행
    │
    ▼
step1_generate.py   ← 전체 오케스트레이터
    │
    ├─ [1단계] naver_news.py        뉴스 크롤링 (제목 + 본문 + URL)
    │
    ├─ [2단계] Gemini AI            원고 생성 (5000자 + 이미지 프롬프트 15개)
    │
    ├─ [3단계] image_fetcher.py     실제 이미지 수집
    │               ├─ 기사 본문 이미지 (슬롯 1~4)
    │               └─ 조감도·평면도  (슬롯 5~9)
    │
    └─ [4단계] Leonardo AI          AI 이미지 생성 (슬롯 10~)
                                    (빈 슬롯만 채움, 실제 이미지 덮어쓰기 안 함)

step2_upload.py     ← 네이버 블로그 자동 업로드 (Playwright)
```

---

## 2. 파일 역할 정의

| 파일 | 역할 |
|------|------|
| `step1_generate.py` | 전체 파이프라인 오케스트레이터. 터미널에서 실행하는 메인 파일 |
| `step2_upload.py` | Playwright로 네이버 블로그 로그인 후 글·이미지 자동 업로드 |
| `naver_news.py` | 네이버 경제 섹션 뉴스 크롤링. `(title, content, url)` 반환 |
| `image_fetcher.py` | 기사 이미지·조감도·평면도 수집 전담 모듈 |
| `expert_instruction.md` | Gemini AI에 전달하는 시스템 프롬프트 (글쓰기 지침 전체) |
| `gemini_writer.py` | 초기 버전 (현재 미사용, 참고용 보존) |
| `naver_uploader.py` | 초기 버전 업로더 (현재 미사용, 참고용 보존) |

---

## 3. 실행 방법

### Step 1: 원고 생성 + 이미지 수집

```bash
cd "c:\다운로드\ai등 공부 자료 및 잡동\네이버 블로그"
python step1_generate.py
```

실행 후 선택 메뉴:

| 번호 | 설명 | 사용 상황 |
|------|------|----------|
| 1 | 네이버 경제 섹션 부동산 뉴스 자동 크롤링 | 매일 자동 운영 |
| 2 | 키워드 검색 뉴스 기반 작성 | 특정 단지·지역 집중 분석 |
| 3 | 자유 주제 기획 작성 | 뉴스 없이 직접 주제 입력 |

결과물: `posts/YYYY-MM-DD/` 폴더에 저장
- `content.txt` — 블로그 원고
- `prompts.txt` — Leonardo AI용 영문 이미지 프롬프트
- `1.jpg ~ N.jpg` — 수집·생성된 이미지

### Step 2: 네이버 블로그 업로드

```bash
python step2_upload.py
```

---

## 4. 이미지 수집 파이프라인 상세

### 슬롯 배분 방식

```
슬롯 1 ~ 4   : 참조 기사 본문 이미지   (실제 뉴스 사진)
슬롯 5 ~ 9   : 조감도 · 평면도 · 배치도  (부동산 특화 실사)
슬롯 10 ~    : Leonardo AI 생성 이미지  (분위기 · 스토리 보완)
```

- 실제 이미지가 있는 슬롯은 Leonardo가 덮어쓰지 않음
- 실제 이미지 수집이 적게 되면 Leonardo가 앞 슬롯부터 채움

### image_fetcher.py 내부 함수

#### `fetch_article_images(news_url, folder_path, start_num, max_images=4)`
- 기사 URL의 `#dic_area img` 태그에서 이미지 추출
- `data-src` → `data-lazy-src` → `src` 순서로 URL 탐색
- 10KB 미만 이미지(아이콘·광고) 자동 스킵
- icon / logo / emoji / blank 패턴 URL 스킵

#### `fetch_realestate_images(content_text, folder_path, start_num, max_images=5)`
- 청약/분양 트리거 키워드 감지 후 실행 여부 결정
- `_extract_apt_names()` 로 단지명 추출 (24개 브랜드 패턴 + 따옴표 패턴)
- 검색 순서: 조감도 → 평면도84 → 평면도59 → 단지배치도 → 모델하우스

#### `_search_naver_images(query, max_urls)`
- 방법 A: 네이버 이미지 검색 JSON API 엔드포인트
- 방법 B: HTML 페이지 스크립트 내 `originalUrl` / `imageUrl` / `thumbnail` 정규식 추출
- 20KB 미만 이미지 스킵 (썸네일·아이콘 제외)

#### `_blur_phone_numbers(image_path)` — 전화번호 자동 제거
- 이미지 다운로드 직후 `_download_single()` 내부에서 자동 호출
- **EasyOCR** (`pip install easyocr`) 로 이미지 내 텍스트 전체 인식
- 인식 텍스트에서 구분자(공백·하이픈·점) 제거 후 정규식 매칭
  - `0\d{1,2}\d{7,8}` — 010·011·02 등 일반 전화
  - `1[5-9]\d{6}` — 1588·1800 등 대표번호
- 전화번호 영역에 **가우시안 블러(반경 25)** 적용 후 덮어쓰기
- EasyOCR 미설치 시 경고만 출력하고 건너뜀 (이미지 다운로드는 정상 진행)
- OCR Reader는 모듈 내 싱글톤으로 최초 1회만 초기화 (이후 실행 빠름)

> **의존성**: `easyocr`, `torch`, `torchvision` (설치 시 약 500MB 소요)
> 첫 실행 시 한국어 모델 자동 다운로드(약 100MB, 30초 소요). 이후 캐시 사용.

---

## 5. 뉴스 크롤링 로직 (naver_news.py)

### get_top_real_estate_news()

```
1. 네이버 경제 섹션 랭킹 접속
   https://news.naver.com/main/ranking/popularDay.naver?sectionId=101

2. 상위 뉴스 목록에서 부동산 키워드 필터링
   ["아파트", "부동산", "청약", "분양", "주택", "전세", "월세",
    "재건축", "재개발", "임대", "매매", "공급", "입주", "단지"]

3. 부동산 기사 없으면 → get_news_by_keyword("아파트 청약 분양") 폴백

4. 기사 본문 추출 (#dic_area → #newsct_article)

5. 반환: (title, content, url)
         ↑ url은 image_fetcher가 기사 이미지 수집에 사용
```

> 변경 이력: 기존에는 전체 뉴스 1위를 무조건 가져와 범죄/정치 뉴스가 입력되는 문제가 있었음.
> 경제 섹션 + 부동산 키워드 필터로 해결 (2026-04-29)

---

## 6. AI 원고 생성 설정

### 모델
- Gemini 2.5 Flash Lite (`gemini-2.5-flash-lite`)
- temperature: 0.7
- max_output_tokens: 8192

### 시스템 프롬프트
- `expert_instruction.md` 전체 내용을 system_instruction으로 전달
- 이 파일을 수정하면 다음 실행부터 즉시 반영됨

### AI 출력 구조 (파싱 구분선)
```
[블로그 원고 5000자]

===CARD_NEWS===
TITLE: ...
SUBTITLE: ...
POINT1: ...
POINT2: ...
POINT3: ...

===IMAGE_PROMPTS===
1. ...
2. ...
...
15. ...
```

---

## 7. 블로그 글 유형별 원고 구성

`expert_instruction.md` 섹션 8~9 에 정의된 표준 구조.

### 주력: 청약·분양 분석 글 (14단계 깔때기)

| 섹션 | 내용 | 목표 분량 |
|------|------|----------|
| 1 | 제목 (키워드 포함) | 1줄 |
| 2 | 핵심 3줄 요약 박스 | 3줄 |
| 3 | 도입 (현장 분위기) | 200~300자 |
| 4 | 단지 기본 정보 | 300~400자 |
| 5 | 입지 분석 (교통·학군·생활·호재) | 500~700자 |
| 6 | 분양가 상세 분석 (핵심) | 600~800자 |
| 7 | 청약 일정·자격 요건 | 300~400자 |
| 8 | 자금 조달 시뮬레이션 | 300~400자 |
| 9 | 수요자별 맞춤 전략 | 400~500자 |
| 10 | 경쟁 단지 비교 | 200~300자 |
| 11 | 전문가 총평 (YES/NO 입장) | 200~300자 |
| 12 | FAQ 3~5개 | 각 100~150자 |
| 13 | CTA | 고정 문구 |
| 14 | 해시태그 | 5개 |

**목표 총 분량: 5,000자 이상**

### 유형별 변형

| 유형 | 주기 | 분량 | 특징 |
|------|------|------|------|
| A. 주간 청약 캘린더 | 주 1회 | 4,000~5,000자 | 3~5개 단지 묶음, 1~2곳 집중 분석 |
| B. 지역 시세 동향 | 격주 | 5,000~6,000자 | 3개월 실거래가 흐름 + 단지 소개 |
| C. 정책·금리·세금 해설 | 월 1~2회 | 4,000~5,000자 | 수치 시뮬레이션 필수 |
| D. 수요자별 현장 Q&A | 격주 | 3,000~4,000자 | 상담 질문 5~7개 Q&A 형식 |

---

## 8. 환경변수 (.env)

```env
GEMINI_API_KEY=...        # Google AI Studio에서 발급
LEONARDO_API_KEY=...      # leonardo.ai에서 발급
NAVER_ID=...              # 네이버 블로그 계정 아이디
NAVER_PASSWORD=...        # 네이버 블로그 계정 비밀번호
```

---

## 9. 절대 규칙 요약

### 블로그 주제 범위
- 다뤄야 할 것: 청약·분양·시세·금리·세금·수요자 전략
- 절대 다루지 않을 것: 범죄·정치·연예·해외 시사
- 판단 기준: "독자가 청약/매수 결정에 직접 활용할 수 있는가?"

### 글쓰기 규칙
- 마크다운 기호 (`#`, `**`, `*`, `>`, `-`) 절대 사용 금지 (네이버 스마트에디터 호환)
- 모바일 가독성: 1~2문장마다 줄바꿈
- 수치 의무화: 분양가·실거래가·3.3㎡당 가격 수치 없는 글 발행 금지
- 명확한 입장: "상황에 따라 다릅니다" 금지, YES/NO 명시

### CTA 규칙
```
"더 자세한 현장 브리핑이나 투자 방향이 궁금하시다면 언제든 편하게 문의주세요.
여러분의 상황에 맞는 최적의 플랜을 함께 고민하겠습니다."
```
부동산 주제 글에서만 사용. 범죄·정치 글에는 절대 사용 안 함 (해당 주제 자체를 작성하지 않으므로 원천 차단).

---

## 10. 폴더 구조

```
네이버 블로그/
│
├── step1_generate.py       ← 메인 실행 파일 (원고 생성 + 이미지 수집)
├── step2_upload.py         ← 네이버 업로드 자동화
├── naver_news.py           ← 뉴스 크롤링
├── image_fetcher.py        ← 실제 이미지 수집 (기사·조감도·평면도)
├── expert_instruction.md  ← AI 글쓰기 시스템 프롬프트
├── 설계서.md               ← 이 파일
│
├── gemini_writer.py        ← 초기 버전 (참고용 보존)
├── naver_uploader.py       ← 초기 버전 (참고용 보존)
│
├── .env                    ← API 키 및 계정 정보 (Git 제외)
├── venv/                   ← Python 가상환경
│
└── posts/
    └── YYYY-MM-DD/
        └── 글제목(최대40자)/   ← 포스트마다 독립 폴더 (같은 날 여러 번 실행 가능)
            ├── content.txt     ← 블로그 원고
            ├── prompts.txt     ← Leonardo AI 이미지 프롬프트
            ├── 1.jpg           ← 기사 이미지 (실제)
            ├── 2.jpg           ← 기사 이미지 (실제)
            ├── ...
            ├── 5.jpg           ← 조감도 (실제)
            ├── 6.jpg           ← 평면도 (실제)
            ├── ...
            └── 10.jpg ~        ← Leonardo AI 생성 이미지

> **다중 포스팅 규칙**: `step2_upload.py` 실행 시 오늘 날짜 폴더 안에 서브폴더가
> 여러 개면 목록을 보여주고 선택하게 함. 1개면 자동 선택.
```

---

## 11. 벤치마킹 블로그 10곳 요약

| 블로그 | 특징 | 우리가 배울 점 |
|--------|------|----------------|
| 늘푸르게 | 재건축 전문, 인문학적 감성 | 수치로 시작하지 말고 감정 건드리는 문장으로 도입 |
| 프리덤 | 치과의사 출신, 직설적 분석 | "살까요 말까요" 명확한 YES/NO 입장 |
| 옥동자 (3.5만 구독) | 역피라미드 글쓰기 | 핵심을 짧게 치고 깊이는 뒤에 쌓기 |
| 엉금엉금 | 젊은 투자자, 솔직한 경험 | 실패 사례 공유로 공감대 형성 |
| 아기곰 | 전문가 저술가 | "초보도 이해할 언어"로 어려운 개념 풀기 |
| 열정로즈 | 청약·분양권 전문 강사 | 청약 전→결과→프리미엄 흐름까지 시리즈 연결 |
| 미네르바올빼미 | 전직 세무공무원 | 글마다 법적 근거·국토부 자료 인용 |
| 신사람 | 중급 이상 상세 분석 | "틀릴 수 있습니다" 겸손한 전문가 어조 |
| mhb-blog | 6,500자 깔때기 구조 | 글 상단에 "핵심 3줄 요약 박스" 배치 |
| 월급쟁이부자들 | 초보자 친화, 커뮤니티 기반 | 댓글 답글로 커뮤니티 활성화 |

**공통 성공 패턴:**
- 1~2개 세부 주제에만 집중
- 수치 기반 분석 (분양가·실거래가·3.3㎡당 가격 필수)
- 청약 일정 기준 1주일 이내 타임라인 유지
- 이미지 10장 이상 (조감도·입지도·시세 캡처)
- 시리즈 구성 (청약 전 분석 → 결과 → 프리미엄 추이)
