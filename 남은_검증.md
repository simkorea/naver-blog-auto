# 남은 검증 — 실제 사건 자료를 넣기 전에

이 문서는 **클로드 코드가 사용자 PC에서 직접 실행할 작업 목록**이다.
컨테이너(웹 세션)에서는 확인할 수 없고, 실제 GPU·실제 라이브러리·실제
네트워크가 있어야만 드러나는 것들만 모았다.

지금까지 실제로 터진 문제는 모두 이 범주였다 — cp949 콘솔, CPU 빌드
덮어쓰기, `use_auth_token`, `Weights only load failed`, speechbrain 의
윈도우 경로 가드, m4a 디코딩. 하나도 코드를 읽어서 미리 찾은 것이 없다.
**돌려봐야 나온다.**

## 규칙

- 한 번에 하나씩. 끝날 때마다 결과를 사용자에게 보고한다.
- 고친 뒤에는 **고치기 전으로 되돌리면 검증이 실패하는지** 확인한다.
- 커밋 전 `python tests/run_all.py` (165항목).
- 사용자의 실제 소송 자료는 아직 넣지 않는다. 전부 만들어낸 자료로 한다.

---

## 1. 오디오 형식 — 가장 급하다

**왜:** 사용자 녹음은 아직 휴대폰에 있다. 어떤 형식일지 모른다.
m4a 가 화자 분리를 죽인 것을 이미 겪었다(`3d01b4a`). 다른 형식도 같은
문제가 있을 수 있고, **밤새 돌린 뒤에 알게 되면 그 시간이 날아간다.**

한국 휴대폰 통화 녹음에서 실제로 나오는 형식:

| 형식 | 어디서 |
|---|---|
| `.m4a` | 삼성 통화 녹음 (가장 흔함) |
| `.3ga` | 삼성 구형 |
| `.amr` | 구형 안드로이드 · 일부 통화 녹음 앱 |
| `.mp3` | 녹음 앱 일반 |
| `.wav` | 일부 녹음기 |
| `.ogg` / `.opus` | 카카오톡 음성메시지 |
| `.aac` | 일부 앱 |

**할 일:** ffmpeg 로 각 형식의 시험용 오디오를 만든다(20~30초, 두 사람이
번갈아 말하는 것처럼 톤을 달리해서). 각각에 대해 **전사 → 화자 분리 →
검색 → 발췌본 추출** 까지 전 과정을 돌린다.

**통과 기준:** 형식마다 4단계가 모두 성공. 실패하면 원인을 실제 라이브러리
소스에서 확인하고 고친다. 지원 못 하는 형식이 있으면 **스캔 단계에서
미리 알려주도록** 만든다 — 전사까지 갔다가 죽으면 안 된다.

**결과 보고:** 형식별 성공/실패 표 + 걸린 시간.

### 이 중 절반은 웹 세션에서 이미 끝냈다 (`tests/test_audio_formats.py`)

ffmpeg 로 12개 형식을 실제로 만들어 넣어 확인했다. 거기서 결함이 나왔다:

  `.3ga`(삼성 통화 녹음)와 `.opus`(카톡 음성메시지)가 `config.AUDIO_EXT` 에
  없어서 **스캔에서 조용히 빠졌다.** 사용자는 녹음이 누락된 줄도 몰랐을 것이다.
  확장자를 넉넉히 채우고, 12개 형식을 매번 실제로 만들어 검증하는 묶음을 넣었다.

**PC 에서 남은 것은 이것뿐이다:** 위 12개 형식에 대해 **실제 Whisper 전사와
실제 pyannote 화자 분리**까지 돌려 본다. 여기(웹)에는 GPU 도 모델도 없어서
분류·WAV 변환까지만 확인했다. m4a 처럼 "변환은 되는데 화자 분리에서 죽는"
경우가 또 있는지는 실제 모델로만 알 수 있다.

---

## 2. 법제처 API — 통째로 미검증

**왜:** `LAW_OC` 키가 이제 설정되어 있는데, **실제 법제처 서버에 요청을
보내 본 적이 한 번도 없다.** 응답 형식 파싱 검증 32항목은 전부 내가
만들어낸 가짜 응답으로 한 것이다. 실제 응답이 다르면 [6 법률 코멘트]
탭 전체가 동작하지 않는다.

**할 일:**
1. `evidence/law/client.py` 로 실제 조회를 한다. 최소한 이 세 가지:
   - 공인중개사법 조문 (사용자 사건의 핵심 법률)
   - 민법 조문 (예: 제565조 계약금)
   - 판례 검색 (예: "중개대상물 확인설명의무")
2. 받은 원문을 `evidence/law/parser.py` 가 제대로 쪼개는지 확인한다.
   조·항·호·목이 살아 있는지, 글자가 깨지지 않는지.
3. `evidence/law/corpus.py` 로 실제로 DB 에 적재해 본다.
4. **인용 검증 게이트**(`verify_citation.py`)가 실제 적재분에 대해
   동작하는지 — 존재하지 않는 조문을 인용하면 막히는지 확인한다.

**주의:** 실제 응답이 예상과 다르면 파서를 고치되, 기존 32항목이 계속
통과해야 한다. 실제 응답 샘플을 `tests/` 에 넣어 회귀 검증으로 만든다.

**통과 기준:** 조문·판례가 실제로 조회·적재되고, [6 법률 코멘트] 탭에서
조문이 보인다. 없는 조문 인용은 차단된다.

---

## 3. 긴 녹음과 중단·재개

**왜:** 사용자는 전체 자료를 **밤새 걸어둘** 예정이다. 두 가지가 확인이
안 됐다 — (a) 1시간짜리가 정말 27분에 끝나는지, (b) 중간에 끄거나
꺼졌을 때 이어서 되는지. `--selftest` 는 20초짜리로만 확인했다.

또 사용자 PC 의 마지막 `--selftest` 에서 **전사 속도 경고("느립니다")가
새로 떴다.** 일시적 부하로 보였지만 확인되지 않았다.

**할 일:**
1. ffmpeg 로 60분짜리 시험용 오디오를 만든다(말소리 형태로).
2. 전사를 돌리고 **실제 소요 시간**을 잰다. GPU 사용률·VRAM 도 함께 본다.
   8GB VRAM 에서 large-v3 + pyannote 가 함께 도는 것이 문제없는지 확인.
3. 처리 도중에 강제로 끊는다(Ctrl+C, 그리고 프로세스 강제 종료 각각).
4. 다시 실행해서 **이어서 처리되는지** 확인한다. 중복 구간이 생기지 않는지,
   DB 가 깨지지 않는지.

**통과 기준:** 60분 전사가 예상 시간 안에 끝나고, 어떻게 끊어도 재실행하면
이어서 되고 중복이 없다. 속도 경고의 실제 원인을 밝힌다.

---

## 4. 대량 처리

**왜:** 사용자 자료가 몇 개인지 모른다. 수십 개일 수 있다.
지금까지 최대 10개 안팎으로만 돌렸다.

**할 일:** 시험용 파일 50~100개(오디오·텍스트·이미지 섞어서)를 만들어
등록하고 전체 처리한다.

확인할 것:
- 화면(Streamlit)이 느려지거나 멈추지 않는지
- 검색 응답 속도 (구간 수만 건 기준)
- 메모리 사용량이 계속 늘어나지 않는지
- 백업 파일 크기와 복원 시간
- 진행 상황이 사용자에게 제대로 보이는지 (몇 개 중 몇 개)

**통과 기준:** 100개를 넣어도 화면이 살아 있고 검색이 몇 초 안에 끝난다.

---

## 5. 카카오톡 내보내기 형식

**왜:** 카톡 내보내기 형식은 PC판과 모바일판이 다르고, 버전에 따라서도
다르다. 지금 파서는 두 형식을 가정하고 있는데(`_PC_MSG`, `_MO_MSG`)
**실제 사용자 카톡으로 확인한 적이 없다.**

**할 일:** 사용자에게 부탁한다 — **소송과 무관한 아무 대화방이나**
(친구와의 대화, 또는 "나와의 채팅") 텍스트로 내보내서 주면 된다.
내용은 볼 필요 없고 **형식만** 확인한다.

- PC 카카오톡에서 하나
- 휴대폰 카카오톡에서 하나

두 개를 각각 파싱해서 날짜·시각·화자·본문이 제대로 나뉘는지 본다.
1MB 넘어 여러 파일로 쪼개진 경우도 확인한다(`find_split_files`).

**통과 기준:** 두 형식 모두 메시지가 빠짐없이 파싱된다. 실제 샘플을
개인정보를 지운 형태로 `tests/` 에 넣어 회귀 검증으로 만든다.

---

## 6. 마지막 — 전체 흐름 한 번

위 다섯 개가 끝나면, 만들어낸 자료로 **사용자가 실제로 밟을 순서 그대로**
한 번 통과시킨다.

자료 등록 → 적법성 지정 → 고유명사 사전 → 전사 → 화자 지정 →
검색 → 발췌 담기 → 타임라인 → 법률 코멘트 → 제출 패키지 → 백업

각 단계에서 **비개발자가 막힐 만한 곳**을 기록한다. 버튼 이름이 헷갈리는지,
다음에 뭘 눌러야 할지 모르겠는지, 오류 메시지가 무슨 뜻인지 모르겠는지.
그것도 결함이다.

**통과 기준:** 처음부터 끝까지 막힘 없이 진행되고, USB 에 넣을 수 있는
제출 폴더가 나온다.

---

## 끝나면

`CLAUDE.md` 의 "지금 상태"를 갱신하고, 사용자에게 **이제 실제 자료를
넣어도 된다**고 알린다. 그때 휴대폰에서 녹음을 옮기는 것부터 시작한다.
